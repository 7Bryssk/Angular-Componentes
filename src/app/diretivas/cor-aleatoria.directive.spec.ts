import { CorAleatoriaDirective } from './cor-aleatoria.directive';

describe('CorAleatoriaDirective', () => {
  it('should create an instance', () => {
    const directive = new CorAleatoriaDirective();
    expect(directive).toBeTruthy();
  });
});
